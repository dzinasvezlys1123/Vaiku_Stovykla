import { CarYearDirective } from './car-year.directive';

describe('CarYearDirective', () => {
  it('should create an instance', () => {
    const directive = new CarYearDirective();
    expect(directive).toBeTruthy();
  });
});
